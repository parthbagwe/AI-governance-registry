# Runtime dependencies for the deployed API.
#
# Kept deliberately lean. The training, drift-detection and dashboard
# libraries (scikit-learn, evidently, streamlit, plotly) are in
# requirements-dev.txt instead — they're only needed on a machine that
# actually trains models, and shipping them would triple the build time and
# the memory footprint of a service whose job is serving JSON.
#
# xgboost, shap and pandas stay here because the /explain endpoint needs
# them at request time to produce SHAP explanations.

# Web / DB
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Needed by the /explain endpoint at request time
pandas>=2.2
numpy>=1.26
xgboost>=2.0
shap>=0.44
