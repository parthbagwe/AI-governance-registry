# Copy this file to `.env` and fill in your own key.
# `.env` is gitignored — never commit a real key.
#
# Free key (800 requests/day, no card required):
#   https://twelvedata.com/pricing
TWELVEDATA_API_KEY=your-key-here
