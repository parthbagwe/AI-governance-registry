# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Database (generated by seed.py — don't commit real/local data)
governance.db
*.db
*.sqlite3

# Trained model artifacts (regenerated by train_model.py)
*.pkl

# Exception: the SME credit model ships with the deployment, because the
# /explain endpoint loads it at request time and a hosted instance has no way
# to train one. This is a documented shortcut — a real setup would pull the
# artifact from a model registry or object store, not from git. Committing a
# build output is a compromise, and it's here because the alternative is a
# deployed demo where explainability silently 503s.
!sme_credit_model.pkl

# Synthetic datasets (regenerated by generate_data.py)
data_train.csv
data_current.csv

# Live-feed snapshots (regenerated by app/ml/live_feed.py — and a stale one
# would be misleading, since the whole point is that it's captured fresh)
live_baseline.csv
intraday_baseline.csv

# API keys. Never commit these — a leaked key is someone else's quota,
# or worse, on a paid tier.
.env
.env.*
!.env.example

# Streamlit
.streamlit/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log

# Frontend (Next.js)
node_modules/
frontend/.next/
frontend/out/
frontend/.vercel
frontend/next-env.d.ts
*.tsbuildinfo
.env
.env.local