# Copy this file to .env.local and adjust as needed.
#
# Local development (FastAPI running via `python -m uvicorn app.main:app --reload`):
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
#
# Once the backend is deployed to Render, set this in Vercel's dashboard
# (Project Settings -> Environment Variables) to something like:
# NEXT_PUBLIC_API_URL=https://ai-governance-registry.onrender.com/api/v1
